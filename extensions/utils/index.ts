/**
 * Pure utility functions belong in this directory.
 *
 * Keep them deterministic: no filesystem, network, Pi UI, or global-state
 * access. Add focused modules beside this file as the extension grows.
 */

export {};
