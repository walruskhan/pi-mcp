/**
 * Side-effectful helpers belong in this directory.
 *
 * Keep integrations with Pi, the filesystem, processes, and the network here;
 * keep their pure data transformations in extensions/utils/.
 */

export {};
